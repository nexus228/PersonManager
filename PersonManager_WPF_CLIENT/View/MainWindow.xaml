<Window x:Class="PersonManager_WPF_CLIENT.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonManager_WPF_CLIENT.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Person Manager" Height="450" Width="800">


    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Button -->

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button 
                Style="{StaticResource SE_ButtonStyle}"    
                Margin="0,0,10,0"
                Content="{StaticResource String_LoadPerson}"
                Width="120"
                Height="30"
                HorizontalAlignment="Left"
                Command="{Binding LoadPersonsCommand}"/>

            <Button 
                Style="{StaticResource SE_ButtonStyle}"    
                Margin="0,0,10,0"
                Content="{StaticResource String_ClearList}"
                Width="120"
                Height="30"
                HorizontalAlignment="Left"
                Command="{Binding ClearListCommand}"/>
        </StackPanel>
        


        <!-- Searchfield with label -->
        <StackPanel Margin="0,10,0,10" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">

            <Label 
                HorizontalContentAlignment="Center" 
                Content="{StaticResource String_SearchPersonForName}"/>
            <TextBox 
                Width="250"
                HorizontalAlignment="Left" VerticalContentAlignment="Center"
                Margin="0,0,0,0"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>
        
       

        <!-- table -->
        <DataGrid Grid.Row="2"
                  Margin="0,10,0,0"
                  ItemsSource="{Binding PersonCollectionView}" 
                  AutoGenerateColumns="False"              
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="OnMouseDoubleClick">
            
            <DataGrid.Columns>

                <DataGridTextColumn
                    Header="{StaticResource String_FirstName}"
                    Binding="{Binding FirstName}"
                    Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Left" />
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Margin" Value="10,0,0,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>

                </DataGridTextColumn>


                <DataGridTextColumn 
                    Header="{StaticResource String_Name}" 
                    Binding="{Binding Name}" 
                    Width="*">
                    
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Left" />
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Margin" Value="10,0,0,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn
                    Header="{StaticResource String_DateOfBirth}"
                    Binding="{Binding DateOfBirth, StringFormat={}{0:dd.MM.yyyy}}"
                    Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Left" />
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Margin" Value="10,0,0,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn 
                    Header="{StaticResource String_Actions}"
                    Width="SizeToCells"
                    MinWidth="125">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>


                            <StackPanel Orientation="Horizontal">


                                <Button
                                    Style="{StaticResource SE_ButtonStyle}"
                                    Command="{Binding DataContext.ShowPersonDetailsCommand,RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}">

                                    <StackPanel>
                                        <Image Source="{StaticResource Image_PersonDetails}" Margin="0,5,0,0"/>
                                        <Label Content="{StaticResource String_Details}" Foreground="White"/>
                                    </StackPanel>

                                </Button>



                                <Button
                                    Style="{StaticResource SE_ButtonStyle}"
                                    Command="{Binding DataContext.EditPersonDataCommand,RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}">

                                    <StackPanel>
                                        <Image Source="{StaticResource Image_EditPerson}" Margin="0,5,0,0"/>
                                        <Label Content="{StaticResource String_Edit}" Foreground="White"/> 
                                    </StackPanel>

                                </Button>


                            </StackPanel>

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>


        </DataGrid>

        <!-- loading indicator -->
        <Border 
            Background="#00FFFFFF" 
            Grid.Row="2" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility"
                            Value="Collapsed"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}"
                                     Value="True">
                            <Setter Property="Visibility"
                                    Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

                <ProgressBar
                    Width="200"
                    Height="15"
                    IsIndeterminate="True"/>

                <TextBlock
                    Text="{StaticResource String_LoadData}"
                    FontSize="18"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0"/>

            </StackPanel>

        </Border>


    </Grid>


</Window>
